<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- 背景颜色 -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- 边框颜色和宽度 -->
    <corners android:radius="4dp" /> <!-- 圆角半径 -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- 内边距 -->
</shape>
